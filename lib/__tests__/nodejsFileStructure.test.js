const assert = require('assert');
const nodejsFileStructure = require('../index.js');

describe('nodejsFileStructure', () => {
  it('has a test', () => {
    assert(false, 'nodejsFileStructure should have a test');
  });
});
